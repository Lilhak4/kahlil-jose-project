
<div class="success apply">
      {{#if applies.length}}
        {{#each applies}}
          <h3>{{this}}</h3>
        {{/each}}
      {{/if}}
    </div>
<div class="page band detail">
    <h3>Name:</h3> <p>{{band.name}}</p>
    <h3>Instruments needed:</h3><p>{{band.instrument_needed}}</p>
    <h3>Members:</h3> <p>{{#each band.member}}{{this.username}} {{/each}}</p>
    <h3>Genre:</h3> <p>{{band.genre}}</p>
</div>
{{#if owner}}
<div class="field">
    <a class="view apply owner" href="/apply/{{band._id}}">View Applications</a>
</div>  
{{/if}}
{{#unless owner}}
{{#unless alreadyApplied}}
<div class="field">
    <form class="apply-form" method="POST" action="/band/apply/{{band._id}}">
        <button type="submit" class="btn-default btn-apply" name="join-btn">Join</button>
    </form>
</div>
{{/unless}}
{{/unless}}    